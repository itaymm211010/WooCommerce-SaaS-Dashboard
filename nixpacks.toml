# Nixpacks configuration for Vite + React
# Force Node.js build instead of Deno

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "sh -c 'npx serve -s dist -l ${PORT:-3000}'"
